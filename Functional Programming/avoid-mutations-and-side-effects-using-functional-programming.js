// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
  let Value = fixedValue+1;
  return Value;

  // Only change code above this line
}
console.log(incrementer())